Favala Female FChatbot - Reduced Web Rig

Assets are extracted directly from the approved transparent component sheet.

Included:
- head_assembly
- body_core
- floating_base
- left_shoulder_module
- right_shoulder_module
- left_forearm_hand
- right_forearm_hand

Each part is supplied as:
- transparent PNG
- SVG container preserving the exact rendered PNG appearance/transparency

The SVG files are web-ready and independently positionable/animatable.
Note: because the source artwork is a rendered 3D raster image, these SVGs preserve the
exact artwork as embedded transparent images; they are not redrawn vector paths.

Suggested layer order:
floating_base -> body_core -> shoulders -> forearms/hands -> head_assembly


Additional facial animation assets:
- left_eye
- right_eye
- nose
- mouth

Each facial part is included as transparent PNG + SVG and can be positioned
independently over/within the head assembly for website facial animation.
